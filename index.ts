import { input, removeDuplicates } from "./src/week4/noduplicates";
import { s1, s2, mergeAlternatively } from "./src/week4/mergealt";
import { arr1, arr2, notPresent } from "./src/week4/notpresent";

console.log(removeDuplicates(input));
console.log(mergeAlternatively(s1, s2));
console.log(notPresent(arr1, arr2));
